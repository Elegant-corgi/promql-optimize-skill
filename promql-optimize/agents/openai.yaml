interface:
  display_name: "PromQL Optimize"
  short_description: "Diagnose PromQL performance and alert semantics with safe range verification."
  default_prompt: "Use $promql-optimize to analyze this PromQL query or alert. Ask before using any real datasource, verify time-based alert behavior with range comparisons when needed, and suggest optimized PromQL plus recording rules."
policy:
  allow_implicit_invocation: true
